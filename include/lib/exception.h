#ifndef EXCEPTION_H
#define EXCEPTION_H

/*
 * Macros to use exception-handling functionality in C.
 * Comes in three parts:
 * 1) A `finally` section at the end of each function to
 *    perform deallocations.
 * 2) Use `raise()` to return a nonzero code
 * 3) You can `try(...)` == `if(!...)` something and then
 *
 *
 *
 *
 */

#endif
